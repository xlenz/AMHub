{
    "PROXY_PORT": 8000,
    "DOCKER_SOCKET": "/var/run/docker.sock",
    "addresses": [],
    "docker": false,
    "network": {
        "dhcp": {
            "net": null,
            "mask": ["relay-", "server-", "dhcp-"]
        },
        "default": {
            "net": "docker0"
        }
    }
}